# Backend URL for the AI chat endpoints (POST /api/ai-action, GET /api/ai-usage).
# Defaults to http://localhost:8787 if unset (matches the Phase 1 demo: Hono on
# 127.0.0.1:8787 — see backend/docs/02-cost-and-ops.md §4.4).
VITE_AI_BACKEND_URL=http://localhost:8787
